var authorization_ctrl = ["$scope", "$http", "prismSessionInfo", "Preferences", "authService", "$uibModal", "NotificationService", "LoadingScreen", "ResourceNotificationService", "$window",
	function ($scope, $http, prismSessionInfo, Preferences, authService, $uibModal, NotificationService, LoadingScreen, RS, $window) {
		'use strict';

	}];

prismApp.controller('authorization_ctrl', authorization_ctrl);